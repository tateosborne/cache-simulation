
# /// @brief returns the word read at the specified address
# /// @param address the 
# /// @return the word read
# Word readWord(unsigned int address);
#     // check if this address is stored in the cache by dividing up the
#     // address into parts to parse it. if it's not in the cache, go out
#     // to memory and retrieve the data and store it in the particular
#     // place in memory

